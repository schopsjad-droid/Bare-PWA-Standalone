/**
 * ListingMap is no longer used — AdDetails shows text-only location.
 * This file is kept as a stub to prevent import errors during migration.
 * It will be deleted in the cleanup phase.
 */
export default function ListingMap() {
  return null;
}
